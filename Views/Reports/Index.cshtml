@{
    var userRole = Context.Session.GetString("UserRole");
    Layout = userRole == "Admin"
        ? "~/Views/Admin/AdminLayout.cshtml"
        : "~/Views/Employee/EmployeeLayout.cshtml";
    ViewData["Title"] = "Reports";
}

@section Styles {
    <link rel="stylesheet" href="~/css/reports.css" asp-append-version="true">
}

<div class="reports-page">

  <!-- Header Section -->
  <div class="page-header">
    <button class="btn-primary" type="button">
      <span class="material-symbols-outlined icon-20">download</span>
      Export Data
    </button>
  </div>

  <!-- Dashboard Style Container -->
  <div class="reports-card">

    <!-- Filter & Search Toolbar -->
    <div class="toolbar">
      <div class="toolbar-left">
        <div class="search-pill">
          <span class="material-symbols-outlined search-icon">search</span>
          <input id="report-search" type="text" placeholder="Search report items...">
        </div>

        <div class="filter-dropdown-wrapper">
          <button class="filter-btn" id="filterBtn" type="button">
            <span id="currentFilter">All Categories</span>
            <span class="material-symbols-outlined">expand_more</span>
          </button>
          <div class="filter-dropdown" id="filterDropdown">
            <button class="filter-option active" data-value="All Categories">All Categories</button>
            <button class="filter-option" data-value="Audit Logs">Audit Logs</button>
            <button class="filter-option" data-value="Archived">Archived</button>
            <button class="filter-option" data-value="Disposed">Disposed</button>
            <button class="filter-option" data-value="Retained">Retained</button>
            <button class="filter-option" data-value="Active Tasks">Active Tasks</button>
            <button class="filter-option" data-value="Completed Tasks">Completed Tasks</button>
          </div>
        </div>
      </div>

      <div class="toolbar-right">
        <button class="icon-btn-outline" type="button" aria-label="Calendar filter">
          <span class="material-symbols-outlined">calendar_today</span>
        </button>
        <button class="icon-btn-outline" type="button" aria-label="More filters">
          <span class="material-symbols-outlined">tune</span>
        </button>
      </div>
    </div>

    <!-- Report List Content -->
    <div class="report-list" id="report-list">
      <!-- Rows are rendered here by reports.js. Empty state shows when there's nothing yet. -->
    </div>

    <!-- Footer Pagination -->
    <div class="pagination-footer">
      <span class="pagination-status" id="pagination-status">Showing 0 of 0 reports</span>
      <div class="pagination-controls" id="pagination-controls"></div>
    </div>
  </div>

</div>

@section Scripts {
    <script src="~/js/reports.js" defer></script>
}
